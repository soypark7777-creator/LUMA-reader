# LUMA 시작하기

이 문서는 로컬 PC에서 LUMA 서버를 실행하고 웹 브라우저로 접속하는 방법을 정리한 가이드입니다.

## 1. 프로젝트 폴더 열기

PowerShell에서 LUMA 프로젝트 폴더로 이동합니다.

```powershell
cd E:\독서모임앱\LUMA
```

## 2. Python 가상환경 만들기

처음 한 번만 실행합니다.

```powershell
python -m venv venv
```

Python이 PATH에 잡혀 있지 않다면 아래처럼 로컬 설치 경로를 직접 사용할 수 있습니다.

```powershell
& "$env:LOCALAPPDATA\Programs\Python\Python312\python.exe" -m venv venv
```

## 3. 가상환경 활성화

```powershell
.\venv\Scripts\activate
```

활성화되면 터미널 앞에 `(venv)`가 표시됩니다.

PowerShell에서 아래 오류가 나오면 실행 정책 때문에 `Activate.ps1`이 막힌 것입니다.

```text
이 시스템에서 스크립트를 실행할 수 없으므로 ... Activate.ps1 파일을 로드할 수 없습니다.
```

이 경우 현재 PowerShell 창에서만 임시로 허용한 뒤 다시 활성화합니다.

```powershell
Set-ExecutionPolicy -Scope Process -ExecutionPolicy Bypass
.\venv\Scripts\activate
```

이 설정은 현재 터미널 창에만 적용되고, 창을 닫으면 원래대로 돌아갑니다.

실행 정책을 바꾸기 싫다면 가상환경을 활성화하지 않고도 아래처럼 직접 실행할 수 있습니다.

```powershell
.\venv\Scripts\python.exe -m pip install -r requirements.txt
.\venv\Scripts\python.exe app.py
```

## 4. 패키지 설치

```powershell
pip install -r requirements.txt
```

가상환경을 쓰지 않고 로컬 Python으로 바로 설치해야 하는 경우:

```powershell
& "$env:LOCALAPPDATA\Programs\Python\Python312\python.exe" -m pip install -r requirements.txt
```

권장 방식은 가상환경입니다.

## 5. .env 설정

이미 `.env` 파일이 있으면 그대로 사용합니다. 없으면 `.env.example`을 복사해서 만듭니다.

```powershell
Copy-Item .env.example .env
```

최소 실행에는 API 키가 없어도 됩니다. 키가 비어 있으면 Mock 모드 또는 샘플 응답으로 동작합니다.

주요 항목:

```env
PORT=5000
SECRET_KEY=luma-super-secret-change-in-production

MYSQL_HOST=localhost
MYSQL_PORT=3306
MYSQL_USER=root
MYSQL_PASSWORD=내_MySQL_비밀번호
MYSQL_DB=luma_db

GEMINI_API_KEY=여기에_실제_키_입력
GOOGLE_MAPS_API_KEY=여기에_Maps_키_입력
GOOGLE_BOOKS_API_KEY=여기에_Books_키_입력
```

MySQL이 실행 중이면 서버 시작 시 자동으로 연결을 시도합니다.

- 연결은 최대 3회 재시도됩니다.
- `luma_db`가 없으면 MySQL 서버에 먼저 연결한 뒤 DB를 생성합니다.
- 연결 성공 시 MySQL 버전이 출력됩니다.
- MySQL 연결에 실패해도 앱은 Mock 모드로 실행됩니다.

## 6. 서버 실행

가상환경이 활성화된 상태에서 실행합니다.

```powershell
python app.py
```

로컬 Python 경로를 직접 사용할 경우:

```powershell
& "$env:LOCALAPPDATA\Programs\Python\Python312\python.exe" app.py
```

정상 실행되면 비슷한 메시지가 보입니다.

```text
LUMA 서버 시작 -> http://localhost:5000/landing
```

## 7. 웹 브라우저 접속

브라우저에서 아래 주소로 접속합니다.

| 주소 | 설명 |
|---|---|
| `http://localhost:5000/landing` | 랜딩 페이지 |
| `http://localhost:5000/` | 별자리 대시보드 |
| `http://localhost:5000/auth/login` | 로그인 / 회원가입 |
| `http://localhost:5000/heart` | 마음 서재 |
| `http://localhost:5000/socrates` | AI 소크라테스 |
| `http://localhost:5000/live` | 라이브 독서방 |
| `http://localhost:5000/social` | 소셜 피드 |
| `http://localhost:5000/community` | 공독의 장 |
| `http://localhost:5000/health` | 서버 상태 확인 |

## 8. 데모 로그인

MySQL 연결과 시드 데이터가 정상 생성되면 데모 계정을 사용할 수 있습니다.

```text
이메일: demo@luma.kr
비밀번호: demo1234
```

MySQL이 없어도 프론트는 `user_demo` 기준 Mock 데이터로 동작합니다.

## 9. 서버 중지

PowerShell에서 서버가 실행 중인 창을 선택한 뒤:

```text
Ctrl + C
```

## 10. 문제 해결

### 포트 5000이 이미 사용 중일 때

`.env`에서 포트를 바꿉니다.

```env
PORT=8080
```

다시 실행 후 아래 주소로 접속합니다.

```text
http://localhost:8080/landing
```

### 한글 또는 이모지가 깨질 때

PowerShell에서 UTF-8 코드페이지를 켭니다.

```powershell
chcp 65001
$env:PYTHONIOENCODING="utf-8"
python app.py
```

### MySQL 연결이 안 될 때

1. MySQL 서버가 켜져 있는지 확인합니다.
2. `.env`의 `MYSQL_HOST`, `MYSQL_PORT`, `MYSQL_USER`, `MYSQL_PASSWORD`, `MYSQL_DB` 값을 확인합니다.
3. 그래도 실패하면 Mock 모드로 실행되므로 웹 화면은 먼저 확인할 수 있습니다.

### 패키지 설치가 꼬였을 때

가상환경을 새로 만드는 것이 가장 깔끔합니다.

```powershell
deactivate
Remove-Item -Recurse -Force .\venv
python -m venv venv
.\venv\Scripts\activate
pip install -r requirements.txt
```

## 11. 빠른 실행 요약

```powershell
cd E:\독서모임앱\LUMA
python -m venv venv
.\venv\Scripts\activate
pip install -r requirements.txt
python app.py
```

브라우저 접속:

```text
http://localhost:5000/landing
```
