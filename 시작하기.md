# LUMA 시작하기

이 문서는 Windows PowerShell에서 LUMA 서버를 켜는 가장 쉬운 방법을 정리한 가이드입니다.

## 1. 프로젝트 폴더로 이동

PowerShell을 열고 아래 명령어를 실행합니다.

```powershell
cd E:\독서모임앱\LUMA
```

현재 위치가 맞는지 확인하려면:

```powershell
Get-Location
```

결과가 `E:\독서모임앱\LUMA`이면 정상입니다.

## 2. Python 확인

현재 PC에서 사용할 수 있는 Python을 확인합니다.

```powershell
py -0p
```

또는:

```powershell
python --version
```

이 프로젝트는 Python 3.12 기준으로 실행하면 가장 안전합니다.

## 3. 기존 venv가 안 될 때 먼저 확인

이미 `venv` 폴더가 있더라도, 다른 PC나 다른 사용자 경로의 Python을 바라보고 있으면 서버가 실행되지 않을 수 있습니다.

예를 들어 이런 오류가 나오면:

```text
No Python at 'C:\Users\User\AppData\Local\Programs\Python\Python312\python.exe'
```

기존 `venv`를 새로 만드는 것이 가장 빠릅니다.

```powershell
Remove-Item -Recurse -Force .\venv
py -3.12 -m venv venv
```

`py -3.12`가 안 되면 아래처럼 실행합니다.

```powershell
python -m venv venv
```

## 4. 가상환경 활성화

```powershell
.\venv\Scripts\activate
```

성공하면 PowerShell 줄 앞에 `(venv)`가 보입니다.

만약 `Activate.ps1` 실행이 막히면, 현재 PowerShell 창에서만 임시로 허용한 뒤 다시 활성화합니다.

```powershell
Set-ExecutionPolicy -Scope Process -ExecutionPolicy Bypass
.\venv\Scripts\activate
```

가상환경을 활성화하지 않고도 실행할 수 있습니다. 문제가 생기면 이 방식이 더 확실합니다.

```powershell
.\venv\Scripts\python.exe -m pip install -r requirements.txt
.\venv\Scripts\python.exe app.py
```

## 5. 패키지 설치

가상환경이 활성화된 상태에서 실행합니다.

```powershell
python -m pip install --upgrade pip
pip install -r requirements.txt
```

가상환경 활성화가 헷갈리면 아래 명령어를 사용하세요.

```powershell
.\venv\Scripts\python.exe -m pip install --upgrade pip
.\venv\Scripts\python.exe -m pip install -r requirements.txt
```

## 6. .env 파일 준비

`.env` 파일이 이미 있으면 그대로 사용합니다.

없으면 예시 파일을 복사합니다.

```powershell
Copy-Item .env.example .env
```

최소 실행에는 API 키가 비어 있어도 됩니다. 일부 기능은 Mock 또는 샘플 데이터로 동작합니다.

주요 설정:

```env
PORT=5000
SECRET_KEY=luma-local-dev-secret

MYSQL_HOST=localhost
MYSQL_PORT=3306
MYSQL_USER=root
MYSQL_PASSWORD=내_MySQL_비밀번호
MYSQL_DB=luma_db

GEMINI_API_KEY=
GOOGLE_MAPS_API_KEY=
GOOGLE_BOOKS_API_KEY=
```

MySQL 연결에 실패해도 앱 일부는 Mock 모드로 실행될 수 있습니다. 다만 로그인, 서재, 라이브, 소크라테스 저장 기능은 MySQL이 켜져 있어야 정상 동작합니다.

## 7. 서버 실행

가장 기본 실행:

```powershell
python app.py
```

가상환경 Python을 직접 지정해서 실행:

```powershell
.\venv\Scripts\python.exe app.py
```

보조 실행 파일로 실행:

```powershell
.\venv\Scripts\python.exe run_luma_server.py
```

`run_luma_server.py`는 `debug=False`, `use_reloader=False`로 실행되므로 서버를 한 번만 안정적으로 띄우고 싶을 때 좋습니다.

정상 실행되면 아래와 비슷한 주소가 표시됩니다.

```text
LUMA 서버 시작 -> http://localhost:5000/landing
```

## 8. 브라우저에서 접속

서버를 켠 PowerShell 창은 닫지 말고 그대로 둡니다.

브라우저에서 아래 주소로 들어갑니다.

```text
http://localhost:5000/landing
```

자주 쓰는 주소:

| 주소 | 설명 |
|---|---|
| `http://localhost:5000/landing` | 랜딩 페이지 |
| `http://localhost:5000/` | 별자리 대시보드 |
| `http://localhost:5000/auth/login` | 로그인 |
| `http://localhost:5000/heart` | 마음 행성계 |
| `http://localhost:5000/socrates` | AI 소크라테스 |
| `http://localhost:5000/live` | 라이브 독서방 |
| `http://localhost:5000/social` | 독자 피드 |
| `http://localhost:5000/community` | 공동체 |
| `http://localhost:5000/health` | 서버 상태 확인 |

## 9. 데모 로그인

MySQL 연결과 시드 데이터 생성이 정상이라면 아래 계정으로 로그인할 수 있습니다.

```text
이메일: demo@luma.kr
비밀번호: demo1234
```

## 10. 서버 끄기

서버가 실행 중인 PowerShell 창에서:

```text
Ctrl + C
```

## 11. 포트 5000이 이미 사용 중일 때

다른 프로그램이 5000번 포트를 쓰고 있으면 `.env`에서 포트를 바꿉니다.

```env
PORT=8080
```

다시 서버 실행:

```powershell
.\venv\Scripts\python.exe app.py
```

브라우저 접속 주소:

```text
http://localhost:8080/landing
```

`.env`를 수정하지 않고 현재 PowerShell 창에서만 8080번으로 임시 실행할 수도 있습니다.

```powershell
$env:PORT="8080"
.\venv\Scripts\python.exe app.py
```

또는 시스템 Python으로 바로 실행합니다.

```powershell
$env:PORT="8080"
python app.py
```

안정 실행용 파일을 사용할 수도 있습니다.

```powershell
$env:PORT="8080"
python run_luma_server.py
```

이 경우 브라우저 주소는 아래처럼 바뀝니다.

```text
http://localhost:8080/landing
```

5000번에 서버가 떠 있는데 `http://localhost:5000/landing`이 404를 반환하면, LUMA가 아닌 다른 서버가 5000번을 사용 중일 가능성이 큽니다. 이때는 위처럼 8080번으로 실행하는 방법이 가장 쉽습니다.

## 12. 가장 추천하는 전체 실행 순서

처음부터 다시 켤 때는 아래 순서대로 실행하세요.

```powershell
cd E:\독서모임앱\LUMA
Remove-Item -Recurse -Force .\venv
py -3.12 -m venv venv
.\venv\Scripts\python.exe -m pip install --upgrade pip
.\venv\Scripts\python.exe -m pip install -r requirements.txt
.\venv\Scripts\python.exe app.py
```

`py -3.12`가 실패하면 이 줄만 바꿔서 실행합니다.

```powershell
python -m venv venv
```

## 13. 빠른 재실행

이미 설치가 끝난 뒤에는 이것만 실행하면 됩니다.

```powershell
cd E:\독서모임앱\LUMA
.\venv\Scripts\python.exe app.py
```

접속:

```text
http://localhost:5000/landing
```
